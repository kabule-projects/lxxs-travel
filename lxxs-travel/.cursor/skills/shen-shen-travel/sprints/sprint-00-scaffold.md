# Sprint 0 · 脚手架

## 前置
无。读取主 Skill 与 `shared/`。

## 产出
- miniprogram app 入口、TS、路由含 tollgate
- cloud/functions 空壳 + 数据库集合说明 `cloud/collections.md`
- `services/api.ts` `utils/event-bus.ts` store
- admin-web 最小 CRUD（items、destinations）
- GM 白名单
- 资源 manifest 类型
- 设备检测 → tollgate

## 验收
开发者工具能编译；login 可写 users；非白名单机进 tollgate。
不要实现玩法页面逻辑。
