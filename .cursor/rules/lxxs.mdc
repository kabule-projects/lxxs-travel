---
description: 旅行小深项目约定。实现小程序、云函数、后台时遵循 Skill 与常量。
globs: "**/*.{ts,js,wxml,wxss,jsx,md}"
alwaysApply: true
---

实现本仓库任何功能前，读取 `.cursor/skills/shen-shen-travel/SKILL.md`。
常量只用 `shared/constants.ts` / `miniprogram/utils/constants.ts`。
完成任务后更新根目录 `PROGRESS.md`。

## 图片规范（强制）

- **所有图片资源统一 WebP**：UI、场景分层、角色、道具 icon、明信片、Loading 按钮与进度条等。
- **禁止**在业务代码或资源目录中使用 `.png` / `.jpg`（除第三方库外）。
- 路径解析使用 `miniprogram/utils/asset-path.ts`（`@2x` / `@3x` 回退）。
- 设计基准 iPhone 17 Pro Max 1320×2868 @3x；同时提供 @2x 以适配高端安卓。
- 布局使用 `rpx` + `safe-area`（见 `utils/device.ts`），避免写死 px 导致机型错位。
