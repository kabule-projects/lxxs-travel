<view class="sprite-wrap" wx:if="{{visible}}">
  <image
    wx:if="{{src && !useFallback}}"
    class="sprite {{floating ? 'sprite--float' : ''}}"
    src="{{src}}"
    mode="widthFix"
    binderror="onError"
  />
  <view wx:else class="sprite-fallback">深深</view>
</view>
